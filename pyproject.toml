[project]
name = "bias-ext-emoji"
version = "0.1.0"
dependencies = ["bias-core>=0.1,<0.2"]
[project.entry-points."bias.extensions"]
emoji = "bias_ext_emoji.backend.ext:extend"
[tool.setuptools.packages.find]
where = ["."]
include = ["bias_ext_emoji*"]
